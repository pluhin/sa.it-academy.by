{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "jenkins-homework.serviceAccountName" . }}
  labels:
    {{- include "jenkins-homework.labels" . | nindent 4 }}
{{- end }}
