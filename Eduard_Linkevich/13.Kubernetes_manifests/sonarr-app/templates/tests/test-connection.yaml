apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sonarr-app.fullname" . }}-test-connection"
  labels:
    {{- include "sonarr-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sonarr-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
