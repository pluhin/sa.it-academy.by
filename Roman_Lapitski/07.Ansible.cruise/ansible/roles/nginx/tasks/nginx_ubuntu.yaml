---
- name: Nginx. Install packages
  apt:
    name: "{{ nginx_package }}"
    state: present
    update_cache: yes

- name: Nginx. Enable and start service
  service:
    name: 'nginx'
    state: started
    enabled: yes
