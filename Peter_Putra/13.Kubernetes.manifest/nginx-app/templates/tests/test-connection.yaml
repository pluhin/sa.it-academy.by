apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-app.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
