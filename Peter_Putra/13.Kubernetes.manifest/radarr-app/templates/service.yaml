apiVersion: v1
kind: Service
metadata:
  name: {{ include "radarr-app.fullname" . }}
  labels:
    {{- include "radarr-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "radarr-app.selectorLabels" . | nindent 4 }}
