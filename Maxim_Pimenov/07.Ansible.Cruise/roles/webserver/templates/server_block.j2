server {
       listen 80;
       listen [::]:80;

       server_name {{ srv_block }};
       root {{ home_dir }}/{{ srv_block }};
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}

