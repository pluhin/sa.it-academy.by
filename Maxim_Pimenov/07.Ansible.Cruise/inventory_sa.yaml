all_workers:
  children:
    work_sa:
      vars:
        site_deb: "local-deb.site"
        site_cent: "local-cent.site"
        virtual_hosts:
          - { name: "{{ site_cent }}", ip: "{{ hostvars[\"host45\"].ansible_host }}"}
          - { name: "{{ site_deb }}", ip: "{{ hostvars[\"host46\"].ansible_host }}"}
      hosts:
        host45:
          ansible_host: 192.168.202.45
        host46:
          ansible_host: 192.168.202.46
jump_sa:
  vars:
    env: "it-academy"
  hosts:
    bastion:
      ansible_host: 178.124.206.53
      ansible_user: jump_sa
