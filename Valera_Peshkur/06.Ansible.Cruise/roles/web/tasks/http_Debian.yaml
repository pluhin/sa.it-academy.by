---
- name: Debian. Install nginx
  apt: ##module install pak in var apt_httpd_packages
    name: "{{ apt_httpd_packages }}"
    state: latest

- name: Debian. Install nginx. Enable and start
  service:
    name: nginx
    state: started
    enabled: yes
