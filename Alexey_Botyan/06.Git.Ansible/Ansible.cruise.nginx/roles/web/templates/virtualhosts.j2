server {
  listen 80;
  listen [::]:80;
  root {{ nginx_www_root }}/{{ item }};
  index index.html index.htm;
  server_name {{ item }} www.{{ item }};
  location / {
    try_files $uri $uri/ =404;
  }
}
