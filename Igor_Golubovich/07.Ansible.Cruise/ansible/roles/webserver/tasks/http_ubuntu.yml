---
- name: Nginx. Install packages
  apt:
    name: "{{ apt_nginx_packages }}"
    state: latest
    update_cache: yes

- name: Nginx. Enable and start service
  service:
    name: nginx
    state: started
    enabled: yes
