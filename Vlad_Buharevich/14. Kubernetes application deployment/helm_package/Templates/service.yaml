apiVersion: v1
kind: Service
metadata:
  name: {{ include "app12.fullname" . }}
  labels:
    {{- include "app12.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app12.selectorLabels" . | nindent 4 }}