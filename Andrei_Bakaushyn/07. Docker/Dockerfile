# Используйте официальный образ Python 3.8-slim как базовый
FROM python:3.8-slim

# Установите рабочую директорию в /app
WORKDIR /app

# Скопируйте текущую директорию в контейнер в /app
COPY . /app

# Установите необходимые зависимости
RUN pip install --no-cache-dir -r requirements.txt

# Сделайте порт 5000 доступным для мира вне контейнера
EXPOSE 5000

# Определите переменную окружения
ENV FLASK_APP=app.py

# Запустите приложение при запуске контейнера
CMD ["flask", "run", "--host=0.0.0.0"]
