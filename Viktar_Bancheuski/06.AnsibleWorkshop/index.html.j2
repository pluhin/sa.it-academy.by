<!DOCTYPE html>
<html>
<head>
    <title>Welcome to {{ current_vhost.name }}</title>
</head>
<body>
    <h1>{{ current_vhost.site_title }}</h1>
    <h2>Hello from {{ ansible_hostname }}!</h2>
    <p>This is virtual host: {{ current_vhost.name }}</p>
    <p>FQDN: {{ ansible_fqdn }}</p>
    {% if hostvars['bastion'] is defined and hostvars['bastion']['env'] is defined %}
    <p>Environment: {{ hostvars['bastion']['env'] }}</p>
    {% endif %}
</body>
</html>