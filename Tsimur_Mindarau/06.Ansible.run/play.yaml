---
- hosts: "{{ group }}"
  #gather_facts: False
  #strategy: free
  # vars:
    # var_01: "Hiiiiiiiiiiiiiiiiiii"
    # var_02: True
    # var_03: 42
  tasks:
    - name: OS version
      debug:
        msg: "{{ ansible_distribution }}/{{ ansible_distribution_version }}"
      tags:
        - os_version
    - name: Mount point/capacity/used
      debug:
        msg: "Mount ({{ansible_mounts[0].mount}}) /{{ansible_mounts[0].size_total}}/{{ansible_mounts[0].size_total - ansible_mounts[0].size_available}}"
      tags:
        - drive
    - name: RAM/capacity/free
      debug:
        msg: "RAM/{{ ansible_memory_mb.real.total }}/{{ ansible_memory_mb.real.free }}"
      tags:
        - memory
    # - name: Print mounts
    #   debug:
    #     msg: "{{ ansible_facts.mounts::fstype }}"
    #   tags:
    #     - mounts





    # - name: Print variables
    #   debug:
    #     msg: "{{ var_01 }} | {{ var_02 }} | {{ var_03 }}"
    #   tags:
    #     - vars
    # - name: Print facts
    #   debug:
    #     msg: "{{ ansible_distribution }} / {{ ansible_distribution_version }}"
    #   tags:
    #     - vars
    # - name: Print facts
    #   debug:
    #     msg: "{{ ansible_facts }}"
    #   tags:
    #     - facts
    # - name: Debug
    #   debug:
    #     msg: "Hi from {{ inventory_hostname }}"
    #   tags:
    #     - all_test
    #     - check::inv
