---
- name: Check public repository redhat basesd system
  shell: "grep -Erhv '^#' /etc/yum.repos.d/* | grep http"
  args:
    executable: /bin/bash
  register: rep
  changed_when: False
- name: output deb public repository
  debug:
    msg: "{{ rep.stdout_lines }}"
