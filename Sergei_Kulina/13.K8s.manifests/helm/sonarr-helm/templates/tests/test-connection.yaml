apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sonarr-helm.fullname" . }}-test-connection"
  labels:
    {{- include "sonarr-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sonarr-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
