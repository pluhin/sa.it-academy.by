apiVersion: v1
kind: Service
metadata:
  name: {{ include "sonarr-helm.fullname" . }}
  labels:
    {{- include "sonarr-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sonarr-helm.selectorLabels" . | nindent 4 }}
