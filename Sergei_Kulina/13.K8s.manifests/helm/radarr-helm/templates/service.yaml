apiVersion: v1
kind: Service
metadata:
  name: {{ include "radarr-helm.fullname" . }}
  labels:
    {{- include "radarr-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "radarr-helm.selectorLabels" . | nindent 4 }}
