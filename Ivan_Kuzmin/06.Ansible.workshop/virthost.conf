server {
   listen 127.0.0.1:80;

   root /var/www/{{ item }}/html;
   index {{ item }}.html {{ item }}.htm;

   server_name {{ item }}  www.{{ item }};

   location / {
       try_files $uri $uri/ =404;
   }
}
