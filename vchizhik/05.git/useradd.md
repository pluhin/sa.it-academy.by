##Useradd 

#Ubuntu
```
sudo useradd -m ansible
sudo passwd ansible
sudo visudo
ansible ALL=(ALL)  NOPASSWD:ALL
```
#Centos
```
sudo useradd -m ansible
sudo passwd ansible
sudo visudo
ansible ALL=(ALL)  NOPASSWD:ALL
```

