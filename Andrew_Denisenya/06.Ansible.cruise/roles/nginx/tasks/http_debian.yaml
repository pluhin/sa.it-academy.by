---
- name: NGINX Install packages
  apt:
    name: "{{ apt_http_packages }}"
    state: present

- name: NGINX Enable and start servce
  service:
    name: nginx
    state: started
    enabled: yes

- name: Copy first page Debian
  copy:
    src: index.html
    dest: "{{ homepage_debian }}"
