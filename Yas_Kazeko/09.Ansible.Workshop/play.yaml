---
- hosts: all
  become: true
  pre_tasks:
    - name: check host
      shell: |
        hostname
      register: out
    - name: show host
      debug:
        msg: "{{ out.stdout }}"
  roles:
    - redmine
