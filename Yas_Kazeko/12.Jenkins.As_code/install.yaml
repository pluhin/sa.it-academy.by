---
- hosts: host_1
  remote_user: updater
  become: yes
  vars:
    packages:
      - nmap
      - speedtest-cli
  tasks:
    - name: Install nmap and speedtest-cli
      pip:
        name: speedtest-cli
        state: present
