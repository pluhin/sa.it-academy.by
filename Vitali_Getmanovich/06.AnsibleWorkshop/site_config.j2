server {
       listen 80;

       server_name {{ item }};

       root /var/www/{{ item }}/html;
       index index.html index.htm;

       location / {
               try_files $uri $uri/ =404;
       }
}
