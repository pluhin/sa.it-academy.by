upstream wordpress {
        least_conn;
        server 127.0.0.1:8080;
        }

server {
  listen 80;

  location / {
        proxy_set_header Host $host;
        proxy_pass       http://wordpress;
  }
}
