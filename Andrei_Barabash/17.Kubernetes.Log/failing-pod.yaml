apiVersion: v1
kind: Pod
metadata:
  name: fail-pod
spec:
  containers:
  - name: fail-container
    image: busybox
    command: ["sh", "-c"]
    args: ["echo 'This pod will fail immediately'; exit 1"]
  restartPolicy: Never
