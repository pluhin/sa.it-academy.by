---
- name: check centos rep
  shell: "grep -Erhv '^#' /etc/yum.repos.d/* | grep http"
  args:
    executable: /bin/bash
  register: rep
  changed_when: False
- name: output centos
  debug:
    msg: "{{ rep.stdout_lines }}"
