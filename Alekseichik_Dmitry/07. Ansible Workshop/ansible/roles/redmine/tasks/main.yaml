---
  #- name: Print inventory 
  #  debug:
  #    msg: "{{ hostvars[inventory_hostname] }}"
    #tags:
    #  - inventory::check
    #  - all_print
  #
  #- name: APT UPDATES   
  #  apt:
  #   update_cache: yes
  
  - name: Setup Debian REDMINE INSTALL PACKS
    include: Packs.yaml
    #when: ansible_os_family == 'Debian'
  
  - name: MySQL CONFIG
    include: MySqlCfg.yaml

  - name: Apache.config
    include: Apache.yaml
  
  - name: FIX REDMINE TMP
    include: Fix_chm.yaml
  
  - name: Check URL 
    include: Check.yaml