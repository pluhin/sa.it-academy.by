---
- name: Check Connect
  uri:
    url: "http://{{ app_fqdn }}"
    return_content: yes
  register: this
  failed_when: "'Jean-Philippe Lang' not in this.content"
  tags: 
   - testweb
  
- debug:
    msg: 
     - "{{ this.url }}"
     - "{{ this.status }}"
     