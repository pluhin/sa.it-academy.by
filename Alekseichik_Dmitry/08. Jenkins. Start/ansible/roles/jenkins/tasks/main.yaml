---
  #- name: Print inventory 
  #  debug:
  #    msg: "{{ hostvars[inventory_hostname] }}"
    #tags:
    #  - inventory::check
    #  - all_print
  #
  - name: Setup. APT UPDATES   
    apt:
     update_cache: yes
  
  - name: Setup.Jenkins REPO
    include: Jenkins_Repo.yaml
    #when: ansible_os_family == 'Debian'


  - name: Setup.Jenkins INSTALL PACKS
    include: Packs.yaml
    #when: ansible_os_family == 'Debian'
  
  
  
  - name: Check.Jenkins URL 
    include: Check.yaml

  - name: Show.Jenkins initialAdminPassword
    include: ShowPass.yaml
  
  
  - name: Generate and Show.Jenkins SSH Keys
    include: ssh_keygen.yaml
  
   