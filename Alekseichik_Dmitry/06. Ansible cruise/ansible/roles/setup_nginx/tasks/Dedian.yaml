---
- name: nginx_Install_for_Debian
  apt:
   name: "{{apt_nginx_list}}" 
   state: latest

- name: nginx_Start Debian
  service: 
   name: nginx  # service name
   state: started  # ask restart
   enabled: yes    # startup check


