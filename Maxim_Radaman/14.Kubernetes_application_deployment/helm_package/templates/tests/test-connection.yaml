apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app12.fullname" . }}-test-connection"
  labels:
    {{- include "app12.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app12.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
