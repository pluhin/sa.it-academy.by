---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-drupal
  namespace: hw-13
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/server-alias: "adev-drupal.k8s-19.sa"
spec:
  rules:
    - host: adev-drupal.k8s-20.sa
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: adev-drupal
                port:
                  number: 80
