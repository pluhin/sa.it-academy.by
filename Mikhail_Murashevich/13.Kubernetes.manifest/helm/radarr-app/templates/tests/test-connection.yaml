apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "radarr-app.fullname" . }}-test-connection"
  labels:
    {{- include "radarr-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "radarr-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
