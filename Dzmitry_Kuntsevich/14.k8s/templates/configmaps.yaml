apiVersion: v1
kind: ConfigMap
metadata:
  name: jenkins-config
  namespace: {{ .Values.namespace }}
data:
  jenkins.yaml: |
    {{- .Files.Get "files/jenkins.yaml" | nindent 4 }}

---

apiVersion: v1
kind: ConfigMap
metadata:
  name: basic-security
  namespace: {{ .Values.namespace }}
data:
  basic-security.groovy: |
    {{- .Files.Get "files/basic-security.groovy" | nindent 4 }}
